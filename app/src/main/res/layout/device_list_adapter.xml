<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="6dp">
  <TextView
    android:id="@+id/paired"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:paddingLeft="@dimen/device_list_title_padding"
    android:paddingRight="@dimen/device_list_title_padding"
    android:layout_alignParentRight="true"
    android:layout_alignParentBottom="true"
    android:text="@string/paired"
    android:textSize="@dimen/device_list_text_size"
    android:textColor="@color/list_secondary_text"/>

  <TextView
    android:id="@+id/rssi"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:paddingLeft="@dimen/device_list_title_padding"
    android:paddingRight="@dimen/device_list_title_padding"
    android:layout_alignParentRight="true"
    android:layout_alignParentTop="true"
    android:layout_below="@+id/paired"
    android:textSize="@dimen/device_list_text_size"
    android:textColor="@color/list_secondary_text"/>

  <TextView
    android:id="@+id/name"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:paddingLeft="@dimen/device_list_title_padding"
    android:paddingRight="@dimen/device_list_title_padding"
    android:layout_alignParentLeft="true"
    android:textSize="@dimen/device_list_name_size"
    android:textColor="@color/list_primary_text"/>

  <TextView
    android:id="@+id/address"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:paddingLeft="@dimen/device_list_title_padding"
    android:paddingRight="@dimen/device_list_title_padding"
    android:layout_below="@+id/name"
    android:textSize="@dimen/device_list_address_size"
    android:textColor="@color/list_secondary_text"/>
</RelativeLayout>